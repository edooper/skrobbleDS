[pytest]
# Confine collection to the real test directory. Without this, any second copy
# of the repo reachable from the working tree (e.g. the local 'docker' symlink
# to the NAS deploy copy) is also walked, and its identically-named test
# modules collide on import - which fails collection for every test file.
testpaths = tests
